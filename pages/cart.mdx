import Main from '../components/Main'
import Banner from '../components/Banner'

<Banner title={`Cart`} />

<div style={{textAlign: `center`, display: `none`}}>
  # Cart
</div>

<section className={`productsSection customSection`}>
  <Main className={`cwsContent`} showTopButton={true}>
    <div className={`pageContentContainer card cardWider`}>
      <h2 className={`subtitle shopSubtitle`}>Your Cart</h2>
      <p style={{ textAlign: `center`, paddingBottom: 5 }}>Cart is Empty.</p>
    </div>
  </Main>
</section>
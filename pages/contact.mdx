import Main from '../components/Main'
import Banner from '../components/Banner'
import Contact from '../components/Contact'

<Banner title={`Contact Us`} bg={`cwsEvent.jpg`} bgPos={`50% 8%`} />

<div style={{textAlign: `center`, display: `none`}}>
  # Contact
</div>

<section id={`contactPage`} className={`contactPageSection productsSection customSection`}>
  <Main className={`cwsContent`} showTopButton={true}>
    <div className={`contactPageCard card cardWider p0Important pageContentContainer overflowXHidden`}>
      <div className={`contactCardContent flex`}>
        <Contact />
      </div>
    </div>
  </Main>
</section>
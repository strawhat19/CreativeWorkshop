import Main from '../components/Main'
import Banner from '../components/Banner'

<Banner title={`About Us`} bg={`cwsTeam.jpg`} />

<div style={{textAlign: `center`, display: `none`}}>
  # About
</div>

<section id={`aboutPage`} className={`aboutPageSection productsSection customSection`}>
  <Main className={`cwsContent`} showTopButton={true}>
    <div className={`pageContentContainer card cardWider`}>
      <h2 className={`subtitle shopSubtitle`}>What is Creative Workshop?</h2>
      <p style={{ textAlign: `left`, paddingBottom: 5 }}>Creative Workshop is a dynamic collective of artists, designers, and fabricators dedicated to bringing your ideas into full color.
      Whether you need a graphic designer or illustrator, help with merch production, or branding and decor for your entire event, Creative Workshop can do it all! 
      No Idea is too "out there".</p>
      <h2 className={`subtitle shopSubtitle`}>This Application</h2>
      <p style={{ textAlign: `left` }}>is a dashboard for tracking orders through Trello Boards, Lists, Cards API and having it be connected directly and programmatically to the Client which communicates back to the TypeForm API to create new orders.</p>
    </div>
  </Main>
</section>